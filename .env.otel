
# used for local development
AWS_REGION=eu-central-1
AWS_ACCESS_KEY_ID=123
AWS_SECRET_ACCESS_KEY=123
AWS_DYNAMODB_TABLE_NAME=service-portal-state-local
AWS_ENDPOINT_URL=http://localhost:5000
ROOT_PATH=/api/wps/v1/state

CORS_ORIGIN_REGEX=http://localhost:\\d+

# moto server port and endpoint for local AWS service emulation (DynamoDB)
# MOTO_PORT is used by docker-compose to expose the container port
MOTO_PORT=5000

# OTEL configuration
OTEL_SDK_DISABLE=false
OTEL_PYTHON_EXCLUDED_URLS=/checker
OTEL_ENABLE_OTLP_EXPORTER=false
OTEL_ENABLE_CONSOLE_EXPORTER=true
OTEL_LOGGING_EXPORTERS=console
OTEL_METRICS_EXPORTERS=console
OTEL_TRACE_EXPORTERS=console

# Metric exporter config
OTEL_ENABLE_METRICS=true
OTEL_METRIC_EXPORT_INTERVAL=10000
OTEL_METRIC_EXPORT_TIMEOUT=3000

# Logging configuration
LOGGING_ENABLE_DEV_SERVER_LOGGING=false
LOGGING_CONFIG_FILE=./logging-otel-config.yaml
